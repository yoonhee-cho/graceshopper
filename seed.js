const { db, Book, Author } = require('./server/db')

const seed = async () => {
    try {
        
    } catch (error) {
        console.log(error)
    }
}

module.exports = seed;

if(require.main === module) {
    seed()
    .then
}